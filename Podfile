# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
pod 'MagicalRecord'

target 'ios-magicalrecord-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-magicalrecord-demo

  target 'ios-magicalrecord-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-magicalrecord-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
